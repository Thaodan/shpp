# These are the option strings that cane be given to shpp via -O <option>


WARNING_IS_ERROR 	# if set all warnings are threated as error
# NOTE: shpp don't takes care if its wise to give options to all slaves especially if given via -O
give_them_all=true 	# true or false, pass all options to slaves  
stdout			# if output goes to stdout
keep			# if set shpp don't deletes $tmp_dir
USE_COLOR		# enable colored output
